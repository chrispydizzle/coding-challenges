namespace CodeChallenges.Sorts
{
    public class

    public int[] SearchRange(int[] nums, int target) {
    // sorted array, let's do a binary search!
    if(nums == null || nums.Length= 0) return new[] {
    };

    internal var result = int[] {
        -1, -1
    }
    while(resut[0] == -1 && resulr[1]
    nums.length/2
    }
}

public class Solution
{
    public int[] SearchRange(int[] nums, int target)
    {
        // sorted array, let's do a binary search!

        if (nums == null || nums.Length = 0) return new[] { };
        var result = int[] {
            -1, -1
        }
        while (resut[0] == -1 && resulr[1]
        nums.length / 2
    }
}